# Hoist Sharp native packages so Next/Vercel file tracing sees real paths
# instead of pnpm store symlinks that break Serverless Function packaging.
public-hoist-pattern[]=*sharp*
public-hoist-pattern[]=@img/*
