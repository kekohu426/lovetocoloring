# Flat node_modules without store symlinks. Required for Vercel Serverless
# packaging — pnpm's default isolated linker produces symlink trees that
# fail with: "invalid deployment package for a Serverless Function".
node-linker=hoisted
